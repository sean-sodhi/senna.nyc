// This file can contain advanced ScrollTrigger setups, timelines, etc.

import { gsap } from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';

gsap.registerPlugin(ScrollTrigger);

export function createScrollAnimations(){
  // Placeholder for advanced scroll animations
  // Example:
  // ScrollTrigger.create({
  //   trigger: ".someClass",
  //   start: "top center",
  //   onEnter: () => { gsap.to(".someClass", {opacity:1}); }
  // });
}
